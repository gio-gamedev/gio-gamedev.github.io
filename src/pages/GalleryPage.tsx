import { ProjectGallery } from '../components/ProjectGallery';

/** The full catalog, with its filters and search; loaded only on /projects/ and /pt/projetos/. */
export default function GalleryPage() {
  return <ProjectGallery />;
}
